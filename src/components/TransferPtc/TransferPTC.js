import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default function TransferPTC() {
    return (
        <View>
            <Text>TransferPTC</Text>
        </View>
    );
}

const styles = StyleSheet.create({});
